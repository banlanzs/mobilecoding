# mytool MVP 3 实施计划

> **For agentic workers:** Use superpowers:subagent-driven-development to implement task-by-task.

**Goal:** 实施 MVP 3 spec 中的 2 个功能：PWA 离线缓存 + Claude 投影深度解析。

**Architecture:**
- PWA：vite-plugin-pwa + Workbox，SPA 壳 Cache First，WebSocket Network Only，会话快照 IndexedDB
- Claude 投影：claude_parser.go 深度解析 JSON，输出结构化 projection.Event

**Tech Stack:** Vite + React + Workbox（前端），Go 标准库（后端）

**Spec Reference:** `docs/specs/2026-06-02-mytool-mvp3-design.md`

---

## Task 29: PWA 基础（vite-plugin-pwa + Workbox）

**Files:**
- Modify: `web/package.json`（新增 vite-plugin-pwa 依赖）
- Modify: `web/vite.config.ts`（配置 PWA）
- Create: `web/src/sw.ts`（Service Worker）
- Create: `web/public/manifest.json`（PWA 清单）

### Step 1: 安装依赖

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool/web" && npm install vite-plugin-pwa workbox-window
```

### Step 2: 配置 vite.config.ts

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.ico', 'robots.txt'],
      manifest: {
        name: 'mytool',
        short_name: 'mytool',
        description: 'AI CLI 远程控制台',
        theme_color: '#222222',
        background_color: '#f5f5f5',
        display: 'standalone',
        start_url: '/',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png',
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png',
          },
        ],
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg}'],
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/.*\/api\/v1\/(healthz|version)/,
            handler: 'NetworkFirst',
            options: {
              cacheName: 'api-cache',
              expiration: {
                maxEntries: 10,
                maxAgeSeconds: 60 * 5, // 5 minutes
              },
            },
          },
        ],
      },
    }),
  ],
});
```

### Step 3: 创建 Service Worker

文件：`web/src/sw.ts`

```typescript
import { precacheAndRoute } from 'workbox-precaching';
import { CacheFirst, NetworkFirst, NetworkOnly } from 'workbox-strategies';
import { registerRoute } from 'workbox-routing';

// Precache all assets generated by Vite
precacheAndRoute(self.__WB_MANIFEST);

// Cache First for static assets
registerRoute(
  ({ request }) => request.destination === 'style' || request.destination === 'script',
  new CacheFirst({
    cacheName: 'static-assets',
  })
);

// Network First for API calls (healthz, version)
registerRoute(
  ({ url }) => url.pathname.startsWith('/api/v1/'),
  new NetworkFirst({
    cacheName: 'api-cache',
  })
);

// Network Only for WebSocket
registerRoute(
  ({ url }) => url.pathname === '/api/v1/ws',
  new NetworkOnly()
);
```

### Step 4: 创建 PWA 清单

文件：`web/public/manifest.json`

```json
{
  "name": "mytool",
  "short_name": "mytool",
  "description": "AI CLI 远程控制台",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#f5f5f5",
  "theme_color": "#222222",
  "icons": [
    {
      "src": "pwa-192x192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "pwa-512x512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

### Step 5: 构建验证

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool/web" && npm run build
```

预期：构建成功，`dist/` 目录包含 Service Worker 文件。

### Step 6: 提交

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool"
git add web/
git commit -m "feat(pwa): 基础 PWA 配置（vite-plugin-pwa + Workbox）"
```

---

## Task 30: PWA 离线快照（IndexedDB）

**Files:**
- Create: `web/src/cache-strategy.ts`
- Modify: `web/src/sw.ts`（集成 IndexedDB）

### Step 1: 实现 cache-strategy.ts

文件：`web/src/cache-strategy.ts`

```typescript
const DB_NAME = 'mytool-sessions';
const STORE_NAME = 'events';
const MAX_EVENTS = 50;

export async function openDB(): Promise<IDBDatabase> {
  return new Promise((resolve, reject) => {
    const request = indexedDB.open(DB_NAME, 1);
    request.onerror = () => reject(request.error);
    request.onsuccess = () => resolve(request.result);
    request.onupgradeneeded = (event) => {
      const db = (event.target as IDBOpenDBRequest).result;
      if (!db.objectStoreNames.contains(STORE_NAME)) {
        db.createObjectStore(STORE_NAME, { keyPath: 'id', autoIncrement: true });
      }
    };
  });
}

export async function saveEvent(event: any): Promise<void> {
  const db = await openDB();
  const tx = db.transaction(STORE_NAME, 'readwrite');
  const store = tx.objectStore(STORE_NAME);
  await store.add({ ...event, timestamp: Date.now() });

  // Keep only last MAX_EVENTS
  const count = await store.count();
  if (count > MAX_EVENTS) {
    const cursor = await store.openCursor();
    if (cursor) {
      await cursor.delete();
    }
  }
}

export async function getRecentEvents(): Promise<any[]> {
  const db = await openDB();
  const tx = db.transaction(STORE_NAME, 'readonly');
  const store = tx.objectStore(STORE_NAME);
  const events: any[] = [];

  return new Promise((resolve, reject) => {
    const request = store.openCursor(null, 'prev');
    request.onsuccess = () => {
      const cursor = request.result;
      if (cursor && events.length < MAX_EVENTS) {
        events.push(cursor.value);
        cursor.continue();
      } else {
        resolve(events.reverse());
      }
    };
    request.onerror = () => reject(request.error);
  });
}

export async function clearEvents(): Promise<void> {
  const db = await openDB();
  const tx = db.transaction(STORE_NAME, 'readwrite');
  await tx.objectStore(STORE_NAME).clear();
}
```

### Step 2: 修改 sw.ts 集成 IndexedDB

```typescript
import { precacheAndRoute } from 'workbox-precaching';
import { CacheFirst, NetworkFirst, NetworkOnly } from 'workbox-strategies';
import { registerRoute } from 'workbox-routing';
import { saveEvent } from './cache-strategy';

// Precache all assets generated by Vite
precacheAndRoute(self.__WB_MANIFEST);

// ... existing routes ...

// Listen for WebSocket messages and save to IndexedDB
self.addEventListener('message', (event) => {
  if (event.data && event.data.type === 'WS_EVENT') {
    saveEvent(event.data.payload);
  }
});
```

### Step 3: 构建验证

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool/web" && npm run build
```

### Step 4: 提交

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool"
git add web/
git commit -m "feat(pwa): IndexedDB 离线快照（最后 50 条事件）"
```

---

## Task 31: Claude 投影深度解析（claude_parser.go）

**Files:**
- Modify: `mytool/internal/engine/claude_parser.go`
- Modify: `mytool/internal/projection/event.go`（新增事件类型）
- Modify: `mytool/internal/engine/claude_parser_test.go`

### Step 1: 扩展 projection/event.go

```go
package projection

import "time"

type EventType string

const (
    EventText             EventType = "text"
    EventLifecycle        EventType = "lifecycle"
    EventToolUse          EventType = "tool_use"
    EventToolResult       EventType = "tool_result"
    EventPermissionReq    EventType = "permission_request"
    EventPlanMode         EventType = "plan_mode"
    EventContextWindow    EventType = "context_window"
    EventSession          EventType = "session"
)

type Event struct {
    Type      EventType
    SessionID string
    Time      time.Time
    Text      string
    Message   string
    // 扩展字段
    ToolName  string
    ToolInput any
    ToolResult any
}

// TextEvent 构造文本事件。
func TextEvent(sid, text string) Event {
    return Event{Type: EventText, SessionID: sid, Time: time.Now().UTC(), Text: text}
}

// LifecycleEvent 构造生命周期事件。
func LifecycleEvent(sid, message string) Event {
    return Event{Type: EventLifecycle, SessionID: sid, Time: time.Now().UTC(), Message: message}
}

// ToolUseEvent 构造工具使用事件。
func ToolUseEvent(sid, toolName string, input any) Event {
    return Event{Type: EventToolUse, SessionID: sid, Time: time.Now().UTC(), ToolName: toolName, ToolInput: input}
}

// ToolResultEvent 构造工具结果事件。
func ToolResultEvent(sid, toolName string, result any) Event {
    return Event{Type: EventToolResult, SessionID: sid, Time: time.Now().UTC(), ToolName: toolName, ToolResult: result}
}

// PermissionRequestEvent 构造权限请求事件。
func PermissionRequestEvent(sid, toolName, prompt string) Event {
    return Event{Type: EventPermissionReq, SessionID: sid, Time: time.Now().UTC(), ToolName: toolName, Message: prompt}
}

// PlanModeEvent 构造计划模式事件。
func PlanModeEvent(sid string, data any) Event {
    return Event{Type: EventPlanMode, SessionID: sid, Time: time.Now().UTC(), ToolInput: data}
}

// ContextWindowEvent 构造上下文窗口事件。
func ContextWindowEvent(sid string, data any) Event {
    return Event{Type: EventContextWindow, SessionID: sid, Time: time.Now().UTC(), ToolInput: data}
}

// SessionEvent 构造会话事件。
func SessionEvent(sid string, data any) Event {
    return Event{Type: EventSession, SessionID: sid, Time: time.Now().UTC(), ToolInput: data}
}
```

### Step 2: 修改 claude_parser.go 深度解析

```go
package engine

import (
    "encoding/json"
    "errors"
)

// ParseClaudeStreamJSON 解析 claude --output-format stream-json 的单行 JSON。
// 深度解析类型，输出结构化 projection.Event。
func ParseClaudeStreamJSON(line []byte) (Event, error) {
    if len(line) == 0 {
        return Event{Kind: EventRaw, Data: nil}, nil
    }
    var m map[string]any
    if err := json.Unmarshal(line, &m); err != nil {
        return Event{}, errors.New("invalid JSON: " + err.Error())
    }

    typ, _ := m["type"].(string)
    // 透传所有 JSON，projection 层负责结构化
    // 这里只做验证，不做深度解析（保持 engine 层轻量）
    cp := make([]byte, len(line))
    copy(cp, line)
    return Event{Kind: EventRaw, Data: cp}, nil
}
```

**关键决策**：engine 层保持轻量（只验证 JSON），projection 层负责深度解析。这样 engine 和 projection 解耦。

### Step 3: 修改 projection/raw.go 集成深度解析

```go
package projection

import (
    "encoding/json"
    "strings"

    "github.com/google/uuid"

    "mytool/internal/engine"
)

// Project 把引擎事件翻译为投影事件。深度解析 Claude stream-json。
func Project(in []engine.Event, sid string) []Event {
    if sid == "" {
        sid = "sess_" + uuid.NewString()
    }
    out := make([]Event, 0, len(in))
    for _, ev := range in {
        switch ev.Kind {
        case engine.EventRaw:
            // 尝试深度解析 Claude stream-json
            parsed, err := parseClaudeEvent(ev.Data, sid)
            if err == nil {
                out = append(out, parsed)
            } else {
                // 非 Claude JSON，透传为文本
                out = append(out, TextEvent(sid, strings.TrimRight(string(ev.Data), "\r\n")))
            }
        case engine.EventLifecycle:
            out = append(out, LifecycleEvent(sid, ev.Message))
        }
    }
    return out
}

// parseClaudeEvent 深度解析 Claude stream-json 事件。
func parseClaudeEvent(data []byte, sid string) (Event, error) {
    var m map[string]any
    if err := json.Unmarshal(data, &m); err != nil {
        return Event{}, err
    }

    typ, _ := m["type"].(string)
    switch typ {
    case "assistant_message":
        msg, _ := m["message"].(string)
        return TextEvent(sid, msg), nil
    case "tool_use":
        name, _ := m["name"].(string)
        input := m["input"]
        return ToolUseEvent(sid, name, input), nil
    case "tool_result":
        name, _ := m["name"].(string)
        content := m["content"]
        return ToolResultEvent(sid, name, content), nil
    case "permission_request":
        toolName, _ := m["tool_name"].(string)
        prompt, _ := m["prompt"].(string)
        return PermissionRequestEvent(sid, toolName, prompt), nil
    case "plan_mode":
        return PlanModeEvent(sid, m), nil
    case "context_window":
        return ContextWindowEvent(sid, m), nil
    case "session":
        return SessionEvent(sid, m), nil
    default:
        return Event{}, errors.New("unknown event type")
    }
}
```

### Step 4: 跑 projection 测试

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool" && go test ./internal/projection/... -v
```

### Step 5: 补 claude_parser_test.go 测试

```go
package engine

import (
    "testing"
)

func TestClaudeParserAssistantMessage(t *testing.T) {
    line := `{"type":"assistant_message","message":"Hello"}`
    ev, err := ParseClaudeStreamJSON([]byte(line))
    if err != nil {
        t.Fatalf("parse: %v", err)
    }
    if ev.Kind != EventRaw {
        t.Errorf("Kind = %q, want raw", ev.Kind)
    }
    if len(ev.Data) == 0 {
        t.Error("Data should not be empty")
    }
}

func TestClaudeParserToolUse(t *testing.T) {
    line := `{"type":"tool_use","name":"Bash","input":{"command":"ls"}}`
    ev, err := ParseClaudeStreamJSON([]byte(line))
    if err != nil {
        t.Fatalf("parse: %v", err)
    }
    if ev.Kind != EventRaw {
        t.Errorf("Kind = %q, want raw", ev.Kind)
    }
}
```

### Step 6: 跑全部测试

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool" && go test ./... -count=1
```

### Step 7: 提交

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool"
git add mytool/internal/projection/ mytool/internal/engine/claude_parser.go mytool/internal/engine/claude_parser_test.go
git commit -m "feat(projection): Claude 投影深度解析（tool_use / permission_request / plan_mode）"
```

---

## Task 32: ws handler 适配新事件类型

**Files:**
- Modify: `mytool/internal/ws/handler.go`
- Modify: `mytool/internal/ws/adapter.go`

### Step 1: 修改 adapter.go 支持新事件类型

```go
package ws

import (
    "encoding/json"

    "mytool/internal/projection"
)

// projectionToEnvelope 把 projection.Event 包装为 ws.Envelope（evt 类型）。
func projectionToEnvelope(p projection.Event) (Envelope, error) {
    raw, err := json.Marshal(p)
    if err != nil {
        return Envelope{}, err
    }
    return Envelope{Type: "evt", SessionID: p.SessionID, Event: raw}, nil
}
```

### Step 2: 验证序列化

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool" && go test ./internal/ws/... -v
```

### Step 3: 提交

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool"
git add mytool/internal/ws/
git commit -m "refactor(ws): adapter 支持新投影事件类型序列化"
```

---

## Task 33: 整体回归 + smoke

### Step 1: 跑全部测试

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool" && go test ./... -count=1
```

### Step 2: 构建验证

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool" && go build -o mytool ./cmd/server
```

### Step 3: 提交

```bash
cd "D:/Documents/Dev-Repo/MobileVC/mytool"
git add -A
git commit -m "test: MVP 3 整体回归"
```

---

## 完成标准

- [ ] PWA 离线缓存配置完成（vite-plugin-pwa + Workbox）
- [ ] IndexedDB 离线快照实现（最后 50 条事件）
- [ ] Claude 投影深度解析完成（tool_use / permission_request / plan_mode）
- [ ] ws handler 序列化新事件类型
- [ ] `go test ./...` 全部 PASS
- [ ] `npm run build` 产出含 Service Worker
